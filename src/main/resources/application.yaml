spring:
  application:
    name: elocate
  datasource:
    url: jdbc:postgresql://db.qnnkizacregmdsfgqrsw.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: AL+4kWTv%A+k9DK
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: kasumalesh@gmail.com
    password: dwqx mclv bkkf soxt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

server:
  port: 8080
  # Context path removed to match frontend API calls
  # servlet:
  #   context-path: /elocate

management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    mail:
      enabled: false  # Disable mail health check

app:
  jwt:
    secret: elocate-super-secret-key-change-in-production-minimum-256-bits-required
    expiration-ms: 86400000 # 24 hours
  otp:
    expiry-minutes: 10
    max-attempts: 3

auth0:
  domain: elocate-dev.us.auth0.com
  clientId: gSMm494WIxE59MtZxODuYvdCRy4yFT10
  clientSecret: NalHurNjv35rQu_zgmR_vkOHB2-OmuOofKBY2lMRUz4Lu3d6F2RQVM9T8REVqHtZ
  connection: Username-Password-Authentication

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
