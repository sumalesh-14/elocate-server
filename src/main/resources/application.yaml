spring:
  application:
    name: elocate
  datasource:
    url: ${DB_URL:jdbc:postgresql://db.qnnkizacregmdsfgqrsw.supabase.co:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:AL+4kWTv%A+k9DK}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  mail:
    host: ${EMAIL_HOST:smtp.gmail.com}
    port: ${EMAIL_PORT:587}
    username: ${EMAIL_USERNAME:kasumalesh@gmail.com}
    password: ${EMAIL_PASSWORD:dwqx mclv bkkf soxt}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

server:
  port: ${SERVER_PORT:8080}
  # Context path removed to match frontend API calls
  # servlet:
  #   context-path: /elocate

management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    mail:
      enabled: false  # Disable mail health check

app:
  jwt:
    secret: ${JWT_SECRET:elocate-super-secret-key-change-in-production-minimum-256-bits-required}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
  otp:
    expiry-minutes: ${OTP_EXPIRY_MINUTES:10}
    max-attempts: ${OTP_MAX_ATTEMPTS:3}

auth0:
  domain: ${AUTH0_DOMAIN:elocate-dev.us.auth0.com}
  clientId: ${AUTH0_CLIENT_ID:gSMm494WIxE59MtZxODuYvdCRy4yFT10}
  clientSecret: ${AUTH0_CLIENT_SECRET:NalHurNjv35rQu_zgmR_vkOHB2-OmuOofKBY2lMRUz4Lu3d6F2RQVM9T8REVqHtZ}
  connection: ${AUTH0_CONNECTION:Username-Password-Authentication}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
